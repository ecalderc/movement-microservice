spring:
  application:
    name: movement
  config:
    import: "optional:configserver:https://config-server-ecalderc.azurewebsites.net"
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      profile: dev